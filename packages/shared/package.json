{
  "name": "@cortex/shared",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "bunx tsc --noEmit",
    "build": "bunx tsc --declaration --emitDeclarationOnly --outDir dist"
  }
}
