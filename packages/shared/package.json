{
  "name": "@cortex/shared",
  "version": "0.5.4",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "bunx tsc --noEmit",
    "build": "bunx tsc --declaration --emitDeclarationOnly --outDir dist"
  }
}
