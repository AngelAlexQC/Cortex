{
  "name": "@cortex/core",
  "version": "0.3.0",
  "description": "Core memory storage and types for Cortex",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target node --format esm && bunx tsc --declaration --emitDeclarationOnly --outDir dist",
    "dev": "bun build src/index.ts --outdir dist --target node --format esm --watch",
    "typecheck": "bunx tsc --noEmit",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage"
  },
  "keywords": [
    "memory",
    "storage",
    "sqlite"
  ],
  "dependencies": {
    "@cortex/shared": "workspace:*"
  },
  "author": "Cortex Team",
  "license": "MIT"
}
