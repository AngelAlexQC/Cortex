# Cursor Rules for Cortex Memory

> Auto-generated by Cortex

1. **Context First**: At the start of a task, ALWAYS search for relevant memory:
   - Use `cortex_search(query)` or `cortex_recall(query)`
   - Review `AGENTS.md` for detailed instructions

2. **Auto-Save**: ALWAYS save important decisions or patterns:
   - Use `cortex_add(content, type, ...)`
   - Types: decision, code, config, fact

3. **Analysis**: If unsure, call `cortex_scan` to understand the project structure.
